# ----- Node / Frontend -----
**/node_modules/
frontend/dist/
frontend/.vite/
frontend/.cache/
frontend/.DS_Store

# keep exactly ONE lockfile depending on your package manager
bun.lockb
pnpm-lock.yaml
yarn.lock

# ----- Python / Backend -----
backend/.venv/
backend/__pycache__/
**/*.pyc
# DB sidecars
backend/city3d.sqlite*
# any venv anywhere
**/.venv/

# Frontend envs (never commit)
frontend/.env
frontend/.env.*
# Misc editor/diagnostic caches
.vscode/
.idea/
.eslintcache
.pytest_cache/
.mypy_cache/
coverage/

# DB & local artifacts
backend/city3d.sqlite

# Env & secrets (never commit)
**/.env
backend/config.py

# Misc
.DS_Store
*.zip
